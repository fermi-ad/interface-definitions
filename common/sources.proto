syntax = "proto3";

package source;

import "google/protobuf/interval.proto";

message Source {
    oneof source {
        google.protobuf.Interval range = 1;
        int32 save_restore = 2;
        string redirection = 3;
    }
}
